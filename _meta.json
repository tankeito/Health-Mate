{
    "name": "health-mate",
    "display_name": "Health-Mate",
    "type": "python/app",
    "version": "1.3.3",
    "description": "Health-Mate is an executable bilingual Chinese/English OpenClaw health-management skill with local markdown parsing, clearer weekly chart labeling, automatic locale-aware English/Chinese PDF generation, and an English memory export helper. SECURITY DISCLOSURE: 1. It requires 'MEMORY_DIR' to be set; if unset, it falls back to reading the default '/root/.openclaw/workspace/memory' path. 2. It writes local reports/logs and may download an open-source TTF font if missing locally. 3. Webhook outbound requests only execute if credentials are user-configured.",
    "author": "tankeito",
    "email": "tqd354@gmail.com",
    "license": "MIT",
    "homepage": "https://github.com/tankeito/Health-Mate",
    "repository": "https://github.com/tankeito/Health-Mate.git",
    "source": "https://github.com/tankeito/Health-Mate",
    "openclaw_version": ">=2024.1.0",
    "python_version": ">=3.8",
    "install": "pip install -r requirements.txt",
    "dependencies": [
        "reportlab>=4.0.0",
        "pillow>=10.0.0",
        "matplotlib>=3.0.0"
    ],
    "capabilities": [
        "file_read",
        "file_write",
        "pdf_generation",
        "http_request"
    ],
    "env": {
        "MEMORY_DIR": "Required. Explicitly set this to the markdown health-memory directory that the skill should read. Production deployment should not rely on wrapper-script fallback paths.",
        "TAVILY_API_KEY": "Optional. Used for Tavily-assisted local fallback in expert commentary, next-day planning, and weekly review suggestions.",
        "DINGTALK_WEBHOOK": "Optional. If set, the final report payload can be sent to DingTalk.",
        "FEISHU_WEBHOOK": "Optional. If set, the final report payload can be sent to Feishu.",
        "TELEGRAM_BOT_TOKEN": "Optional. If set together with TELEGRAM_CHAT_ID, the final report payload can be sent to Telegram.",
        "TELEGRAM_CHAT_ID": "Optional. Required only when Telegram delivery is enabled.",
        "REPORT_WEB_DIR": "Optional. Local directory where generated PDFs can be copied for public serving.",
        "REPORT_BASE_URL": "Optional. Public base URL used to build downloadable PDF links."
    },
    "commands": [
        {
            "trigger": "/health",
            "description": "Generate the localized daily Health-Mate report.",
            "usage": "/health [date]",
            "example": "/health 2026-03-15"
        },
        {
            "trigger": "/health summary",
            "description": "Generate the localized weekly Health-Mate review.",
            "usage": "/health summary [date]",
            "example": "/health summary 2026-03-15"
        }
    ],
    "features": [
        "Executable local Python scripts and shell runners, not an instruction-only prompt bundle",
        "Bilingual Chinese/English parsing, prompts, and report generation",
        "Strict memory-write protocol for reliable markdown parsing",
        "Localized daily and weekly PDF reports with charts, multi-condition support, and fallback-safe report generation",
        "Custom scoring modules and additional monitoring sections including medication and lab-style sections",
        "Optional outbound network behavior limited to Tavily, webhook delivery, and fallback font download when needed"
    ],
    "tags": [
        "health",
        "wellness",
        "diet",
        "hydration",
        "fitness",
        "openclaw",
        "bilingual",
        "pdf",
        "webhook",
        "local-files"
    ]
}
